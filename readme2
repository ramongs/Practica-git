0k lets see how it works


Este es un ejemplo para la comunidad de..

una vez instalas git, debes configurarlo

git config --global user.name "yigly"
git config -- global user.mail "xxx@mail.com"

Generando tu Public Key
your public key

ssh-keygen

leyendo tu llave para copiartla a GitHub:

cat ~/.ssh/id_rsa.pub

Arrancando tu proyecto:

git init

Touch Readme

git add Readme 

git commit -m "tu primer commit"

git push origin master

SSH Public key


oorale deja checar lo ultimo